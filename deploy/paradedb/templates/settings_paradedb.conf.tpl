# ParadeDB Settings (LobeChat recommended Postgres)
INSTANCE_NAME="{{INSTANCE_NAME}}"

# Host Port
DB_PORT="{{DB_PORT}}"

# Database Credentials
DB_USER="{{DB_USER}}"
DB_PASSWORD="{{DB_PASSWORD}}"
DB_NAME="{{DB_NAME}}"
